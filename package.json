{
  "name": "notioncook",
  "version": "1.0.0",
  "dependencies": {
    "@notionhq/client": "^2.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
